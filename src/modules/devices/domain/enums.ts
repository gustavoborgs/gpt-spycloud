export enum DeviceStatus {
  INACTIVE = 'INACTIVE',
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
  MAINTENANCE = 'MAINTENANCE',
}

export enum DeviceType {
  TRACKER = 'TRACKER',
  SENSOR = 'SENSOR',
  GATEWAY = 'GATEWAY',
}

